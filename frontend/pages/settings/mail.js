/**
 * Старый адрес экрана почты. Почта переехала внутрь модуля уведомлений вкладками
 * (владелец 16.09.2026): рассылка — один модуль, и правила с текстом письма не должны
 * лежать в разных пунктах меню.
 *
 * Страница оставлена редиректом, а не удалена: адрес полтора месяца жил в закладках и в
 * письмах, а ссылку, уже отправленную человеку, переписать нельзя. Тот же довод, по
 * которому в карте навигации живут 21 легаси-правило.
 */
import { useEffect } from 'react'
import { useRouter } from 'next/router'

export default function MailMoved() {
  const router = useRouter()
  useEffect(() => { router.replace('/settings/notifications') }, [router])
  return null
}
