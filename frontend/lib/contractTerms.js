/**
 * Условия оплаты договора — один список на весь проект.
 *
 * Был объявлен дважды: в `pages/directory/contracts.js` и в
 * `components/mobile/ContractsMobile.js`. Третья копия появилась бы в форме заведения
 * доходного договора на сборке ОРД — а в этом проекте продублированные константы уже
 * расходились (формула НДС в четырёх копиях, форматтеры денег в тринадцати). Здесь
 * расхождение стоит дороже обычного: условие оплаты уезжает в акт ОРД.
 *
 * Значения сравниваются в базе буквально — это текст в `contracts.payment_term_condition`,
 * а не код. Менять написание существующих нельзя: старые строки перестанут совпадать.
 * Поэтому реестр показывает незнакомое значение как «(старое)», а не прячет его.
 */
export const PAYMENT_TERM_CONDITIONS = ['С даты УПД', 'С даты АКТ', 'По периоду']
