# Сервис кабинета паблишера. Набор нарочно короче, чем у финмодуля: сюда не должны
# попадать pandas/openpyxl/Pillow — кабинет не строит отчётов и не жмёт картинки.
# Каждая лишняя зависимость во внешнем контуре — это поверхность атаки без применения.
fastapi==0.118.0
uvicorn==0.29.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
PyJWT==2.13.0
bcrypt==4.1.3
# Кабинет не пишет в базу — он зовёт ядро по внутренней сети.
httpx==0.27.2
# Приём медиакита: без него FastAPI падает при СТАРТЕ на объявлении
# `UploadFile`, а не при первом запросе — и контур не поднимается вовсе.
python-multipart==0.0.31
pytest==8.2.2
ruff==0.6.9
