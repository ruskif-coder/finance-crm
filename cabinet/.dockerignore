# Что НЕ должно попадать в образ (аудит 23.09.2026, 1.L10): до этого в образ уезжали
# локальные логи разработки (logs/backend.log — 1,5 МБ, с путями и данными стенда) и
# кеши. `tests/` остаётся: по нему идёт проверка на стенде (`docker exec cabinet_backend … pytest`).
logs/
**/__pycache__/
**/*.pyc
.pytest_cache/
.ruff_cache/
.env
*.sql
