# Build artifacts
node_modules/
.next/
__pycache__/
*.pyc

# Claude Code internal folder — credentials, history, plugin cache
.claude/

# Secrets — never commit
.env
.env.*

# DB dumps and backups — real data, local only
backups/

# Backend logs (bind-mounted in docker-compose.yml)
backend/logs/

# Windows deploy/operational scripts — local only
*.bat

# Internal docs and planning files — local only
CLAUDE.md
AUDIT.md
IMPLEMENTATION_PLAN.md
docs/

# SQL migration scripts — applied manually, local only
*.sql

# One-off utility scripts (moved to scripts/ folder)
make_*.py
scripts/

# Stray data exports
/*.xlsx
/*.xls
/*.csv
*.md

# Keep DEPLOYMENT.md and README if present
!DEPLOYMENT.md
!README.md

# OS
.DS_Store
Thumbs.db
