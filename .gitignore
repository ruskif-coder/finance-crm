# Build artifacts
node_modules/
.next/
__pycache__/
*.pyc

# Secrets — never commit
.env
.env.*

# Локальные дампы и бэкапы БД — большие файлы с реальными данными, хранятся локально, не в git
backups/

# Логи backend (смонтированы с хоста в docker-compose.yml) — операционные данные, не код
backend/logs/

# Разовые выгрузки с реальными данными в корне проекта
/*.xlsx
/*.xls

# OS
.DS_Store
Thumbs.db
