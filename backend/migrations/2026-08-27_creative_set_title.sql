-- Название креатива: номер остаётся номером, имя даёт человек.
--
-- Номер (`no`) присваивается по порядку и меняться не должен: на него ссылаются журнал
-- действий, уведомления и код пары. Имя — свободный текст для человека («ролик 15 сек»,
-- «баннеры декабрь»), и правится он отдельно, чтобы правка подписи не трогала нумерацию.
ALTER TABLE launch_prep_creative_set ADD COLUMN IF NOT EXISTS title text;
