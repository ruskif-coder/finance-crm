-- Два системных профиля рассылки, которых не было: «Трафик» и «Паблишеры».
--
-- Замер 30.08.2026: профилей пять (аккаунт, мастер аккаунт, сейлз, финансы, админ), а
-- ролей, не сопоставленных ни с одним, — три: Траффик, Мастер траффик и Менеджер
-- паблишеров. Скрипт раскладки честно писал «роль не сопоставлена — пропуск», и люди
-- этих ролей оставались в профиле по умолчанию, то есть получали чужой набор.
--
-- Профиль отвечает за КАНАЛЫ, а не за адресатов: кому уходит событие, объявлено в самом
-- событии (`app/notify/registry.py`). Поэтому профиль без подписок ничего не отключает —
-- он просто не даёт событию канала, и человек не видит ничего.
--
-- Подписки НЕ здесь: они считаются от реестра скриптом
-- `python -m app.notify.seed_profiles`, иначе разъедутся с кодом. Тут только сами
-- профили — их ключи нужны и миграции, и скрипту.

INSERT INTO notification_profiles (key, label, description, is_system, is_default)
SELECT 'traffic', 'Трафик',
       'Материал на проверку, молчание своих. Медиапланы и сделки сюда не входят.',
       true, false
WHERE NOT EXISTS (SELECT 1 FROM notification_profiles WHERE key = 'traffic');

INSERT INTO notification_profiles (key, label, description, is_system, is_default)
SELECT 'publishers', 'Менеджер паблишеров',
       'Молчание площадок и их ответы по креативам — разговор с площадкой ведёт он.',
       true, false
WHERE NOT EXISTS (SELECT 1 FROM notification_profiles WHERE key = 'publishers');
