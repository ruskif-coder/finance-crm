-- Цвет услуги: маркер услуги на дашборде аккаунта, в реестре сделок и в МП.
-- Согласовано с владельцем 2026-08-17.
--
-- NULL — не «нет цвета», а «взять авто из палитры» (app/sales/colors.py): цвет считается
-- детерминированно от имени услуги, поэтому маркер осмыслен сразу, ещё до того как кто-то
-- что-то выберет руками. Заданное значение перебивает авто.
--
-- varchar(7) — только hex вида '#2FA37C'. Выбор в интерфейсе идёт из палитры, а не
-- свободным вводом: иначе через год в справочнике будет сорок оттенков серого.

ALTER TABLE sales_services ADD COLUMN IF NOT EXISTS color varchar(7);
