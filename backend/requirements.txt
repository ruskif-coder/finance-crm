# 2026-07-18 (пентест): fastapi 0.111→0.118.0 (тянет starlette 0.48.0, макс. совместимую)
# и python-multipart 0.0.20→0.0.31 — закрывают CVE в парсинге multipart/form (DoS при
# загрузке файлов) и большинство starlette 0.x. Проверено: 33/33 backend-теста + чистая
# сборка образа. starlette НЕ пиним явно — fastapi сам резолвит совместимую (>=0.40,<0.49);
# явный пин 0.49.1 ломал сборку (fastapi 0.118 требует <0.49). python-jose удалён ранее
# (миграция на PyJWT) — тянул ecdsa/rsa, уходят при пересборке.
# ОСТАТОК: часть starlette-CVE фиксится только в starlette 1.x — мажорная миграция,
# вынесена в отдельное тестовое окно (см. docs/PENTEST_PLAN.md).
fastapi==0.118.0
uvicorn==0.29.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
alembic==1.13.1
PyJWT==2.13.0
passlib==1.7.4
bcrypt==4.1.3
python-multipart==0.0.31
pandas==2.2.2
openpyxl==3.1.5
Pillow==12.3.0
python-dotenv==1.2.2
pytest==8.2.2
httpx==0.27.2