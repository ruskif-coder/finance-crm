"""Контур верификатора Weborama (WCM) — отдельный, как Битрикс, ОРД и DSP.

Weborama измеряет, засчитан ли показ: видимость, фрод, brand safety. Её пиксель — то самое
поле `pixel` у креатива DSP, помеченное в их доке «Обязателен для ротации!»: без него
креатив заводится, но крутиться не начинает.

Что здесь есть сейчас: клиент (`client`) и правила имён с сборкой тега (`naming`).
Чего ещё нет и почему — хранение (соответствие «наша сущность → их id», журнал попыток,
пул выданных аккаунтов) ждёт согласования схемы с владельцем, а живая проба — учётных
данных и ответа их менеджера про `ad_space_id`: метод создания ad_space в доке
отсутствует, а без него вставку не завести.
"""
