"""Учётки кабинета паблишера — со стороны ЯДРА.

Здесь только администрирование: завести, привязать площадки, сбросить пароль, отключить.
Сам кабинет — отдельный сервис (`cabinet/` в корне репозитория), он ходит в базу под
своей ролью и этот код не импортирует.

Мастер данных — ядро. Кабинет учётки только читает (`pub.account_v1`) и умеет ровно одну
запись — отметку времени входа, и ту через функцию `pub.touch_login`, а не грантом на
таблицу: грант открыл бы `hashed_password` и `is_active`, то есть кабинет мог бы сам себя
включить обратно после отключения.

Таблицы созданы миграцией backend/migrations/2026-08-28_publisher_cabinet.sql.
"""
