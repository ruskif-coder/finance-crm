"""
Маппинг (воронка, стадия Битрикса) → слой денег.

Раскладка хранится в таблице bitrix_stage_map, а не в коде: названия стадий
в портале содержат имена сотрудников («Закрывающие документы | Мария»,
«Подготовка и согласование ДС (задача на Наде)») и меняются при кадровых
перестановках. Правка маппинга не должна требовать деплоя.

Исходная логика — GetCWValue_Fast в modFillinClearData.bas текущего Excel-файла,
где та же раскладка зашита в код по каждой воронке отдельно.
"""
from app.sales.normalize import normalize_name

MONEY_LAYERS = ("планируемые", "реализуемые", "фактические")

# Канонический «светофор» 2/2/2: шесть стадий сделки, по две на каждый слой денег.
# Ключи и порядок совпадают с фронтендом (StageBar) — менять только синхронно с ним.
# Привязка стадии Битрикса к одной из этих позиций задаёт и stage_key (для StageBar),
# и слой денег (для P&L/отчётов). Правка тут = правка светофора для всей системы.
STAGE_CATALOG = [
    {"key": "media_plan",   "label": "Медиаплан",             "money_layer": "планируемые"},
    {"key": "booking",      "label": "Бронь",                 "money_layer": "планируемые"},
    {"key": "launch_prep",  "label": "Сбор запуска",          "money_layer": "реализуемые"},
    {"key": "launch",       "label": "Запуск",                "money_layer": "реализуемые"},
    # Факт разделён на две позиции (2026-08-17): готовим документы и документы в обороте.
    # До этого шесть стадий документооборота имели один ключ, и светофор их не различал.
    {"key": "closing",      "label": "Закрытие подготовка",   "money_layer": "фактические"},
    {"key": "closing_fact", "label": "Закрытие фактическое",  "money_layer": "фактические"},
    # Архив — ТЕРМИНАЛЬНЫЙ исход, а не седьмая позиция бара: закрытая сделка вышла из
    # конвейера, и рисуется зелёной штриховкой по всей полосе (как срыв — красной).
    # Ключ за ним сохранён: он живёт в sales_bitrix_stage_map, в фильтре «скрыть архив»
    # и определяет слой денег 963 архивных сделок.
    {"key": "archive",      "label": "Архив",                 "money_layer": "фактические",
     "terminal": True},
]
STAGE_BY_KEY = {s["key"]: s for s in STAGE_CATALOG}
# Шесть позиций светофора — каталог без терминальных. Фронтенд (STAGE_ORDER в
# salesTableKit) обязан совпадать с этим списком по составу и порядку.
LIGHT_KEYS = [s["key"] for s in STAGE_CATALOG if not s.get("terminal")]

# Нижний уровень каскада SLA (дни): сколько сделка может стоять на стадии этого
# под-этапа, если ни стадия, ни этап срок не задали. Держим здесь, а не в базе,
# чтобы система считала срочность для ВСЕХ стадий сразу после миграции — незаполненный
# sla_days не должен молча выключать проверку. 0 — срока нет по смыслу этапа:
# «Запуск» — РК идёт, стоять нормально; «Архив» — терминальная.
# Значения из ТЗ дашборда аккаунта (docs/«кабинет аккаунта v1»), свёрнутые к шести позициям.
SLA_DEFAULTS = {
    "media_plan": 2,
    "booking": 5,
    "launch_prep": 3,
    "launch": 0,
    "closing": 5,
    "closing_fact": 5,
    "archive": 0,
}


def build_stage_index(rows) -> dict:
    """Строит индекс из строк bitrix_stage_map.

    Ключ — пара нормализованных (воронка, стадия): у разных воронок разные названия
    стадий, ведущие в один слой, поэтому ключом не может быть одна стадия.
    Значение — слой денег как есть, без нормализации: он сравнивается литерально
    в отчётах и обязан совпадать с одним из MONEY_LAYERS."""
    index = {}
    for r in rows:
        key = (normalize_name(r.pipeline), normalize_name(r.bitrix_stage))
        index[key] = r.money_layer
    return index


def resolve_money_layer(index: dict, pipeline, stage) -> str | None:
    """Возвращает слой денег или None, если пара неизвестна.

    None — значимый результат, а не ошибка: неизвестная стадия не относится
    ни к одному слою и должна попасть в очередь сопоставления, а не молча
    в «планируемые». Витрина показывает такие сделки в корзине «Без группы»,
    чтобы итог по слоям сходился с общим итогом."""
    if pipeline is None or stage is None:
        return None
    return index.get((normalize_name(pipeline), normalize_name(stage)))
