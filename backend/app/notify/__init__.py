"""Уведомления: один модуль на оба контура.

Собран воедино 14.09.2026 по решению владельца: «дорабатывать не кусками». До этого
внутренний контур жил здесь, внешний — в `app/cabinet`, а доставка была спрятана внутри
шины приватными функциями, до которых внешний контур дотянуться не мог и потому сочинял
свои.

## Карта

    registry.py     ЧТО бывает внутри: 26 событий сотрудникам — ключ, тон, кнопка, повтор
    recipients.py   КОМУ внутри: резолверы («аккаунт сделки», «контур трафика»)
    bus.py          событие от кода: подписка → профиль → умолчание, и в каналы
    scanner.py      события ОТ СОСТОЯНИЯ: 14 правил, ищутся по расписанию
    dispatch.py     досылка того, что легло в очередь
    channels.py     КАК доходит: колокольчик · бот · почта, плюс тихие часы
    telegram.py     транспорт бота
    models.py       подписки, профили, каналы пользователя, журнал доставок

    outward/        ВНЕШНИЙ контур — площадки (своя дверь, см. его шапку)
        kinds.py        каталог 16 видов: что объявлено и что построено
        send.py         прослойка отправки площадке
        schedule.py     тихие часы и час дайджеста по времени площадки

Транспорт почты общий и живёт отдельно — `app/mail/`: он обслуживает не только
уведомления (запрос посадочной — не уведомление), и втягивать его сюда значило бы
сделать модуль зависимым от всей переписки компании.

## Правило, которое держит конструкцию

**КОМУ и ЧТО решает контур, КАК — общий канал.** Внутренний знает про профили и
телеграм, внешний — про контакты площадки и её часовой пояс; но разметку письма, журнал
и определение «сейчас или позже» они делят. Как только доставка снова расползётся по
контурам, у одного вопроса появится два ответа — этим модуль и болел.

## Что в нём не работает на сегодня

· сканер не стоит в cron на проде — 14 правил из 26 молчат с рождения;
· дайджест: поля есть, обработчика нет;
· внешний контур: построено 4 вида из 16.

Подробности — `docs/SCHEMA_рассылка_два_контура.md` и `docs/SPEC_состав_уведомлений.md`.
"""
from app.notify.bus import emit           # noqa: F401
from app.notify import registry           # noqa: F401
from app.notify.outward import notify_publisher   # noqa: F401
